//Is the script linked to the index.html file in order to:

// Adds an event listener to the document to execute the callback
// only after the DOM content has fully loaded and is ready to be interacted with.
document.addEventListener("DOMContentLoaded", () => {
    // Logs a message to the console indicating that the frontend script is running.
    console.log("Front-end is running!");
});
